#!/bin/sh
# Portable sway launcher — detects NVIDIA and adds --unsupported-gpu
# Uses full paths because display managers (SDDM) have minimal PATHs.

if /usr/bin/lspci | /usr/bin/grep -qi nvidia; then
    exec /usr/bin/sway --unsupported-gpu "$@"
else
    exec /usr/bin/sway "$@"
fi
