# Worklog: WRK-004-6 - Profile-scoped OpenCode setup

**Unit:** WRK-004-6
**Route:** PROCEED
**Status:** done

## Plan

- Keep `opencode-web` registered for both Linux profiles; run its existing config migration before Stow, then provision the newly linked user unit after Stow.
- Require OpenCode and a working user systemd manager for both Linux profiles; require a connected Tailscale client and configure the persistent loopback Serve proxy only for `linux-workstation`.
- Remove Tailscale from the shared `oc` attach path so `linux-remote` remains local-only and does not require a tailnet.
- Update the root operations documentation without staging its independent E2E edits.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) — VERIFIED — `justfile` invokes `setup-opencode-web` before `all`, so the user unit cannot yet be stowed when service provisioning runs; `oc` unconditionally requires Tailscale.
- **PB-SENSE** (still makes sense) — VERIFIED — `CHANGE.md` Done when still requires reliable Linux OpenCode setup, while the user-directed profile distinction has not been encoded.
- **PB-PLAN** (plan achieves impact, in bounds) — RESOLVED-BY-JUDGMENT — move the existing setup call after `all`, pass the resolved profile to the existing script, and branch Tailscale only there; this preserves the shared service contract and gives remote hosts a local-only attach path.
- **PB-CONTRACT** (contract/types still sound) — VERIFIED — `_profile-context` already emits only `linux-workstation` and `linux-remote` for Linux, and the setup script is the private recipe boundary for the new profile argument.

## Judgment calls

- Should `oc` retain a Tailscale prerequisite? → No (high confidence) — it attaches only to the loopback service, and a global prerequisite would violate the local-only remote profile even when setup is correct.

## Where it left off

- Completed: `just setup` migrates config before Stow, then provisions the user service after Stow for both Linux profiles; workstation alone validates Tailscale and configures Serve; `oc` no longer requires Tailscale. Validation passed: `just --list`, workstation `plan` and `check-conflicts`, shell syntax, `systemd-analyze --user verify`, `git diff --check`, and an isolated mock-host test proving remote migration succeeds with a failing Tailscale command while workstation provisioning issues the exact Serve command. Remote `plan` and `check-conflicts` report the pre-existing `/home/sirhamy/.bash_profile` conflict.
