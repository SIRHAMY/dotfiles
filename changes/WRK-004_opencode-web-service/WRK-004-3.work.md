# Worklog: OpenCode interactive helper

**Unit:** WRK-004-3
**Route:** PROCEED
**Status:** done

## Plan

- Add one Bash helper at the stowed local-bin path, leaving the committed service and defaults unchanged.
- Validate the pinned OpenCode binary, required tools, Tailscale connection, private environment ownership and permissions, and the user systemd manager before starting the service.
- Parse only the documented password and optional username assignments without sourcing the file; keep Basic-auth credentials in a mode-restricted temporary curl config while polling health, then export them only for the interactive attach process.
- Before every authenticated health probe, require the `Type=exec` service to remain active so a failed or restarting unit cannot redirect credentials to another loopback listener.
- Bypass inherited proxies for the authenticated loopback health probe and remove HTTP(S)/all-proxy variables before the local attach client starts.
- Exercise the helper with syntax checks and temporary-home command doubles that cover successful attach and prerequisite failures.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) — VERIFIED — `packages/linux/opencode-web/` has the committed `opencode-web.service`, `config.json`, and `server.env.example`, but no stowed `oc` helper; Checklist item 3 remains unchecked.
- **PB-SENSE** (still makes sense) — VERIFIED — `CHANGE.md` is In Progress and its Done-when condition that `oc` starts or reuses the service and attaches the current worktree remains unmet.
- **PB-PLAN** (plan achieves impact, in bounds) — VERIFIED — `opencode-web.service` uses `~/.opencode/bin/opencode`, loopback port 4096, and `~/.config/opencode/server.env`; the helper can validate that same contract, use authenticated local health, and invoke the Decision-2 interactive attach command without changing the service/defaults.

## Where it left off

- Added the executable `oc` helper only at the stowed Linux local-bin path; service and defaults remain unchanged.
- `bash -n` passes. Temporary-home command doubles cover proxy-bypassed health then proxy-free interactive attach, inactive-service rejection before curl, temporary curl-config removal, missing OpenCode/curl/tailscale, unavailable and disconnected Tailscale, absent/invalid/insecure private env files, and inaccessible user systemd.
- Self-review fixed explicit removal of the credential-bearing curl config before `exec`, because an `EXIT` trap alone does not run when `exec` replaces the shell.
