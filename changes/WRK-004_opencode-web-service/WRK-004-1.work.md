# Worklog: OpenCode secret boundary and defaults

**Unit:** WRK-004-1
**Route:** PROCEED
**Status:** done

## Plan

- Add only the public lower-precedence OpenCode defaults to the Linux package; no secret-bearing file is stowed.
- Add a Linux setup hook before Stow that preserves higher-precedence config, reports its `permission` or `share` keys without displaying values, and creates or validates the private environment contract.
- Migrate a conflicting regular `config.json` locally only after validating JSON; back it up, then move it to `opencode.json` or merge it beneath a valid existing `opencode.json`.
- Register the package in both Linux profiles so the public default reaches its intended target; retain the existing profile-specific conflict preflight.
- Use shell syntax and temporary-home verification instead of RED-first tests because this is a configuration/migration setup unit with no existing shell test harness.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) — VERIFIED — `justfile` has no OpenCode setup hook and `packages/linux/` has no OpenCode config; `CHANGE.md` Decision-9 requires the missing lower-precedence defaults.
- **PB-SENSE** (still makes sense) — VERIFIED — the Done-when secret and config-preservation conditions remain unmet; `CHANGE.md` is In Progress.
- **PB-PLAN** (plan achieves impact, in bounds) — VERIFIED — OpenCode v1.18.3 `config.ts` loads `config.json`, then `opencode.json`, then `opencode.jsonc`; the prescribed public default and report-only setup hook preserve that order and Decision-4/Decision-9.

## Where it left off

- Public defaults and the setup hook are complete. `bash -n`, JSON assertions, `just --show`, temporary-home creation/reuse checks, and the symlink rejection check pass.
- Gate fix: the Just recipe invokes the non-executable setup script through `bash`; both Linux package lists include `opencode-web`, and temporary-home `check-conflicts`/`plan` checks cover it.
- Gate fix: a public `server.env.example` documents the local-only contract, and `config.json` migration validates before backing up and moving or atomically merging active settings.
- Migration checks cover config-only move, precedence-preserving merge, invalid JSON, unavailable `jq`, unchanged `opencode.jsonc`, and both profile plans/conflict preflights.
- Light staged-diff review found no blocking issue; `auth` is the required specialist tag because the unit establishes a secret-file contract.
- `just --fmt --check` was not a valid clean gate because it reports pre-existing whole-file formatting changes; the recipes themselves parse successfully.
