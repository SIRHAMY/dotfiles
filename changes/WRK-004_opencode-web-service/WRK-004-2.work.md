# Worklog: OpenCode systemd user service

**Unit:** WRK-004-2
**Route:** PROCEED
**Status:** done

## Plan

- Add the user service at the `opencode-web` package path that Stow maps to `~/.config/systemd/user/opencode-web.service`.
- Load the private server environment only with systemd's `EnvironmentFile`, then run the documented per-user OpenCode executable on loopback port 4096.
- Use `Type=exec`, failure-only restart supervision, and control-group termination; validate the unit and both registered Linux package plans without reading secret contents.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) — VERIFIED — `packages/linux/opencode-web/` contains only OpenCode defaults and the environment example; no user service is stowed.
- **PB-SENSE** (still makes sense) — VERIFIED — `CHANGE.md` remains In Progress, with this service checklist item unchecked and its Done-when service conditions unmet.
- **PB-PLAN** (plan achieves impact, in bounds) — VERIFIED — `justfile` registers `opencode-web` in both Linux profiles; `CHANGE.md` Decisions 1, 3, 4, and 6 require one supervised loopback service, a private environment file, and `serve`.

## Where it left off

- The service is stowed at `~/.config/systemd/user/opencode-web.service` for both Linux profiles.
- Gate 2 added `Type=exec`, so a missing or non-executable pinned OpenCode binary fails the service start immediately.
- `systemd-analyze verify --user`, directive assertions, both `just plan` targets, and `git diff --check` pass without reading the private environment file.
