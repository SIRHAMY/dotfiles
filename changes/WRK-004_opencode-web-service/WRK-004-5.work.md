# Worklog: WRK-004-5 - E2E smoke verification

**Unit:** WRK-004-5
**Route:** PROCEED
**Status:** left-off

## Plan

- Exercise `oc` prerequisite failure branches against disposable homes and command mocks, preserving the active host configuration and never reading the private password.
- Exercise setup migration branches in disposable homes, then provision the authorized user service, verify authenticated health through `oc`'s safe configuration path, and inspect service and Tailscale state.
- Record each check only to the evidence available on this host. Do not enter an interactive TUI, log out, alter tailnet policy, or assert external phone/ACL access without an external-device result.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) - VERIFIED - `CHANGE.md` leaves the e2e checklist item open, and live service behavior still requires host evidence.
- **PB-SENSE** (still makes sense) - VERIFIED - The remaining done condition requires live host behavior beyond the static checks recorded in `WRK-004-1.work.md` through `WRK-004-4.work.md`.
- **PB-PLAN** (plan achieves impact, in bounds) - VERIFIED - `scripts/setup-opencode-web.sh` supports isolated HOME migration tests; `packages/linux/opencode-web/.local/bin/oc` has deterministic prerequisite and authenticated-health paths; the user authorized the exact provisioning commands.
- **PB-CONTRACT** (contract/types still sound) - VERIFIED - This unit exercises existing shell and system-service contracts without changing an exported contract or service implementation.
- **PB-SHAPE** (service shape sound) - VERIFIED - This unit changes no backend logic; it verifies the existing systemd edge described by `opencode-web.service`.

## Where it left off

- Passed in disposable homes: exact named failures for missing OpenCode, `curl`, Tailscale, and an insecure private environment file; migration move and deep-merge paths, timestamped backups, and unchanged `opencode.jsonc`.
- Authorized provisioning completed: `just setup-opencode-web`, `just _stow-bucket linux opencode-web`, user daemon reload, lingering, and enablement. `Linger=yes`; `opencode-web.service` is enabled and active.
- The user removed the manual server. The managed service now starts, remains active after restart, and returns authenticated local health through the same retry logic used by `oc`.
- Tailscale HTTPS and operator access were enabled, then `tailscale serve --bg http://127.0.0.1:4096` published the private route. The user confirmed the phone Web UI loads.
- Unverified: interactive attach and idempotence, shared Web/TUI session visibility, post-logout availability, and ACL exclusion.
- Validation: `bash -n` passed for the setup script and `oc`; `systemd-analyze --user verify` passed for the stowed unit; `git diff --check` passed. `just --fmt --check` remains nonzero because it proposes pre-existing whole-file `justfile` formatting changes.
