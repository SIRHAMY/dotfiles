# Worklog: OpenCode Web operations documentation

**Unit:** WRK-004-4
**Route:** PROCEED
**Status:** done

## Plan

- Add one root-README operations section for the Linux-only service: prerequisites, setup and migration behavior, persistent user service, private Tailscale access, normal use, recovery, archival, and an unrun manual smoke procedure.
- Ground commands in the stowed unit, `oc` helper, setup script, installed CLI help, and current Tailscale Serve and policy documentation; use a labeled policy placeholder rather than inventing this tailnet's device identifiers.
- Mark this documentation checklist item only; retain the e2e item because the procedure is documented, not run on a real host.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) — VERIFIED — `CHANGE.md` checklist item 5 is unchecked, while root `README.md` has no OpenCode Web operations section.
- **PB-SENSE** (still makes sense) — VERIFIED — `CHANGE.md` Done when still requires a documented Linux setup, shared Web/TUI use, private-tailnet access, recovery, and logs; the e2e procedure remains unrun.
- **PB-PLAN** (plan achieves impact, in bounds) — VERIFIED — `justfile` registers `opencode-web` for both Linux profiles; `setup-opencode-web.sh`, `opencode-web.service`, and `oc` establish the exact setup, secret, service, and attach contracts. Tailscale's Serve documentation confirms `tailscale serve --bg http://127.0.0.1:4096`, and its policy reference grounds an illustrative device-to-host HTTPS rule.

## Where it left off

- Root `README.md` now covers installation, config migration and override warnings, linger and systemd lifecycle, restricted Tailscale Serve, normal `oc` use, recovery, archives, logs, and the unrun e2e procedure.
- `CHANGE.md` marks only the documentation item complete; the e2e item remains unchecked because no real-host smoke was run.
- `git diff --check` completed without output. No `markdownlint` or `markdownlint-cli2` executable is installed, so Markdown was reviewed structurally; the intended files are ready to stage and the unrelated untracked change-local `README.md` remains untouched.
